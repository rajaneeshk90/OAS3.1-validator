apiVersion: v1
kind: ConfigMap
metadata:
  name: oas31-validator-config
  namespace: beckn-spec
data:
  # Add your application configuration here
  LOG_LEVEL: "info"
  PORT: "3000"
  # Add other configuration variables as needed 