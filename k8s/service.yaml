apiVersion: v1
kind: Service
metadata:
  name: oas31-validator-service
  namespace: beckn-spec
  labels:
    app: oas31-validator
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: oas31-validator 